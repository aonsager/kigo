# Secrets — API keys must never be committed.
.env

# Regenerable workflow caches — not source of truth.
# The reviewable spine (spine-2026.csv), the scripts, and the README ARE tracked.
spine_pool.json
descriptions.csv
images.csv
# fetch_images.py `fetch` output — one row per image candidate for human review,
# then consumed by `select`. Regenerable working state, not source of truth.
candidates.csv
batches/
responses/
downloads/
__pycache__/

# SQLite editorial review store (ADR 0025) — working state, not source of truth.
review.db
review.db-wal
review.db-shm
